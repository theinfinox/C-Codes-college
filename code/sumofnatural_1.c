//pogram to sum of first n natural num
#include <stdio.h>

int main ()
{
  int pat,i,s=0;
  printf ("Enter number of number of numbers: ");
  scanf ("%d", &pat);
  for(i=1;i<=pat;i++)
 { 
     s=s+i;
     
     
 }
 printf("%d",s);
  return 0;
}
